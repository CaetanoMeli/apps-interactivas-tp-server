const gamesModel = require('./Games');
const equationModel = require('./Equation');
const countFastModel = require('./CountFast');
const numberPatternModel = require('./NumberPattern');
const ratingModel = require('./Rating');

module.exports = {
    gamesModel,
    equationModel,
    countFastModel,
    numberPatternModel,
    ratingModel
};